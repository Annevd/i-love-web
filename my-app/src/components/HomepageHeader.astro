<h1>
	<div class="container">
		<div class="line"></div>
		<span class="name">Anne van Dijk</span>
	</div>
	<img src="./images/banner.png" alt="">
	<div class="container">
		<span class="subject">Portfolio</span>
		<div class="line"></div>
	</div>
</h1>

<p>About me, my frontend work, We-love-web's i've visited and all of my other creations.</p>


<style>

.name {
    font-size: clamp(2rem, 5vw + 1rem, 5rem);
    white-space: nowrap;
}

img {
    width: 100%;
    object-fit: cover;
    max-height: 21rem;
    margin: 1rem 0;
}

.subject {
    font-size: clamp(3rem, 10vw + 1rem, 7rem);
    text-transform: uppercase;
}

.container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
}

.line {
    width: 100%;
    height: 1px;
    background: var(--accent-color);
}

p {
    max-width: 30rem;
}
</style>